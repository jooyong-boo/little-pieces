generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// TODO: User, CoupleSpace, CoupleMember, Memory 모델을 이후 단계에서 추가합니다.
